import { createContext } from "react";

export const LoadingContext = createContext(null);
export const SetFavoriteMessageContext = createContext(null);
export const ThemeContext = createContext(null);
